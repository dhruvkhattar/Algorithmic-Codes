#include<stdio.h>

typedef long long int ll;

ll x[5000006];

int main()
{
		int i;
		for( i=2 ; i<=5000005 ; i++ )
		{
				if(x[i]==0)
				{
						int j;
						for()
				}
		}
		int t;
		scanf("%d",&t);
		while(t--)
		{
				int a,b;
				scanf("%d%d",&a,&b);
				printf("%lld",x[a]-x[b]);
		}
		return 0;
}
